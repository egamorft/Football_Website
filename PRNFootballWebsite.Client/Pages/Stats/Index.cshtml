@page
@model PRNFootballWebsite.Client.Pages.Stats.IndexModel
@{
    ViewData["Title"] = "Statistic";
}

<div class="section-title single-result" style="background:url(img/locations/3.jpg)">
    <div class="container">
        <div class="row">

            <div class="col-lg-12">
                <div class="result-location">
                    <ul>
                        <li>
                            @Model.StatisticData.Datetime.ToString("HH:mm:ss dd/MM/yyyy")
                        </li>
                        <li>
                            <i class="fa fa-map-marker" aria-hidden="true"></i>
                            @Model.StatisticData.Stadium
                        </li>
                        <li>Att: 80,000</li>
                    </ul>
                </div>
            </div>
        </div>


        <div class="row">
            <div class="col-md-5 col-lg-5">
                <div class="team">
                    <img style="width: 20%" src="~/img/clubs-logos/@Model.StatisticData.Team1Logo" alt="club-logo">
                    <a asp-page="/Team/Index" asp-route-id="@Model.StatisticData.Team1ID">@Model.StatisticData.Team1Name</a>
                    <ul>
                        @foreach (var item in Model.StatisticScorer)
                        {
                            @if (item.TeamName.Equals(Model.StatisticData.Team1Name))
                            {
                                var score_mins = "";
                                if (item.ScoreMinutes > 90)
                                {
                                    score_mins = "90(+" + (item.ScoreMinutes - 90) + ")";
                                }
                                else
                                {
                                    score_mins = item.ScoreMinutes.ToString();
                                }
                                <li>@item.PlayerName @score_mins <i class="fa fa-futbol-o" aria-hidden="true"></i></li>
                            }
                        }
                    </ul>
                </div>
            </div>
            <div class="col-md-2 col-lg-2">
                <div class="result-match">
                    @Model.StatisticData.Team1Goal : @Model.StatisticData.Team2Goal
                </div>
                <div class="live-on">
                    <a href="#">
                        Live on
                        <img src="~/img/img-theme/espn.gif" alt="">
                    </a>
                </div>
            </div>
            <div class="col-md-5 col-lg-5">
                <div class="team right">
                    <a asp-page="/Team/Index" asp-route-id="@Model.StatisticData.Team2ID">@Model.StatisticData.Team2Name</a>
                    <img style="width: 20%" src="~/img/clubs-logos/@Model.StatisticData.Team2Logo" alt="club-logo">
                    <ul>
                        @foreach (var item in Model.StatisticScorer)
                        {
                            @if (item.TeamName.Equals(Model.StatisticData.Team2Name))
                            {
                                var score_mins = "";
                                if (item.ScoreMinutes > 90)
                                {
                                    score_mins = "90(+" + (item.ScoreMinutes - 90) + ")";
                                }
                                else
                                {
                                    score_mins = item.ScoreMinutes.ToString();
                                }
                                <li>@item.PlayerName @score_mins <i class="fa fa-futbol-o" aria-hidden="true"></i></li>
                            }
                        }
                    </ul>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                <div class="timeline-result">
                    <div class="team-timeline">
                        <img src="~/img/clubs-logos/@Model.StatisticData.Team1Logo" alt="club-logo">
                        <a asp-page="/Team/Index" asp-route-id="@Model.StatisticData.Team1ID">@Model.StatisticData.Team1Name</a>
                    </div>
                    <ul class="timeline">
                        @foreach (var item in Model.StatisticScorer)
                        {
                            @if (item.TeamName.Equals(Model.StatisticData.Team1Name))
                            {
                                <li class="card-result top goal" style="left:@item.ScoreMinutes%" data-placement="top" data-trigger="hover" data-toggle="popover" title="Goal" data-content="@item.PlayerName">
                                    @item.ScoreMinutes'
                                </li>
                            }
                            else
                            {
                                <li class="card-result bottom goal" style="left:@item.ScoreMinutes%" data-placement="bottom" data-trigger="hover" data-toggle="popover" title="Goal" data-content="@item.PlayerName">
                                    @item.ScoreMinutes'
                                </li>
                            }

                        }
                    </ul>
                    <div class="team-timeline">
                        <img src="~/img/clubs-logos/@Model.StatisticData.Team2Logo" alt="club-logo">
                        <a asp-page="/Team/Index" asp-route-id="@Model.StatisticData.Team2ID">@Model.StatisticData.Team2Name</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<section class="content-info">

    <div class="single-team-tabs">
        <div class="container">
            <div class="row">

                <div class="col-xl-12 col-md-12">

                    <ul class="nav nav-tabs" id="myTab">
                        <li class="active"><a href="#stats" data-toggle="tab">Mach Stats</a></li>
                    </ul>

                </div>
                <div class="col-lg-12">

                    <div class="tab-content">


                        <div class="tab-pane active" id="stats">

                            <div class="row match-stats">
                                <div class="col-lg-5">
                                    <div class="team">
                                        <img style="width: 20%" src="~/img/clubs-logos/@Model.StatisticData.Team1Logo" alt="club-logo">
                                        <a asp-page="/Team/Index" asp-route-id="@Model.StatisticData.Team1ID">@Model.StatisticData.Team1Name</a>
                                    </div>
                                </div>
                                <div class="col-lg-2">
                                    <div class="result-match">
                                        VS
                                    </div>
                                </div>
                                <div class="col-lg-5">
                                    <div class="team right">
                                        <a asp-page="/Team/Index" asp-route-id="@Model.StatisticData.Team2ID">@Model.StatisticData.Team2Name</a>
                                        <img style="width: 20%" src="~/img/clubs-logos/@Model.StatisticData.Team2Logo" alt="club-logo">
                                    </div>
                                </div>
                                <div class="col-lg-12">
                                    <ul>
                                        <li>
                                            <span class="left">@Model.StatisticData.Team1Possession</span>
                                            <span class="center">Possession %</span>
                                            <span class="right">@Model.StatisticData.Team2Possession</span>
                                        </li>
                                        <li>
                                            <span class="left">@Model.StatisticData.Team1Ontarget</span>
                                            <span class="center">Shots On Target</span>
                                            <span class="right">@Model.StatisticData.Team2Ontarget</span>
                                        </li>
                                        <li>
                                            <span class="left">@Model.StatisticData.Team1Shoot</span>
                                            <span class="center">Shots</span>
                                            <span class="right">@Model.StatisticData.Team2Shoot</span>
                                        </li>
                                        <li>
                                            <span class="left">@Model.StatisticData.Team1Corner</span>
                                            <span class="center">Corner</span>
                                            <span class="right">@Model.StatisticData.Team2Corner</span>
                                        </li>
                                    </ul>
                                </div>
                            </div>

                        </div>

                    </div>

                </div>
            </div>
        </div>
    </div>

</section>
